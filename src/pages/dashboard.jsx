import { Box, Typography } from "@mui/material";

export default function Dashboard(){
    return (
        <Box>
            <Typography component='h2' variant="h2">Dashboard</Typography>
        </Box>
    )
}